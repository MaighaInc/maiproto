services:

  postgresDB:
    ports:
      - "5434:5432"
    environment:
      POSTGRES_DB: testdb

  nodeapi:
    environment:
      NODE_ENV: ${NODE_ENV}
      DATABASE_URL: ${DATABASE_URL}
    command: sh -c "npx prisma migrate deploy && npm test"